# 字相 Agent 使用指南

> **字即是相，相即是字。**
> 本指南帮助你从零开始运行「字相」Agent，完成姓名人像艺术创作。

---

## 目录

1. [环境准备](#1-环境准备)
2. [安装项目](#2-安装项目)
3. [配置 API 密钥](#3-配置-api-密钥)
4. [两种运行模式](#4-两种运行模式)
   - [Web 服务模式（推荐）](#41-web-服务模式推荐)
   - [CLI 命令行模式](#42-cli-命令行模式)
5. [使用 Web 界面](#5-使用-web-界面)
6. [画风说明](#6-画风说明)
7. [多 API 配置详解](#7-多-api-配置详解)
8. [常见问题](#8-常见问题)

---

## 1. 环境准备

| 依赖 | 版本要求 | 说明 |
|------|---------|------|
| Rust 工具链 | edition 2024（Rust ≥ 1.85） | 安装：[https://rustup.rs](https://rustup.rs) |
| API 密钥 | OpenAI 或兼容服务 | 用于视觉分析、文本推理、图片生成 |
| 浏览器 | 任意现代浏览器 | Chrome / Edge / Firefox 均可 |

**验证 Rust 安装：**

```bash
rustc --version   # 应显示 rustc 1.85.0 或更高
cargo --version    # 应显示 cargo 1.85.0 或更高
```

---

## 2. 安装项目

```bash
git clone https://github.com/lantingxu2025/zixiang-agent
cd zixiang-agent
```

**验证项目结构：**

```
zixiang-agent/
├── src/
│   ├── main.rs                    # 入口：CLI + serve 模式
│   ├── lib.rs                     # 模块导出
│   ├── config.rs                  # 多 API 适配器
│   ├── server.rs                  # axum Web 服务
│   ├── types.rs                   # 数据结构
│   ├── agent/loop.rs              # Agent 主循环（端到端 pipeline）
│   ├── knowledge/character_db.rs # 汉字部首→视觉元素知识库
│   └── tools/                     # 五大工具
│       ├── face_analysis.rs      #   视觉分析
│       ├── name_analysis.rs      #   汉字分析
│       ├── composition_planner.rs#   构图规划
│       ├── prompt_generator.rs   #   Prompt 生成
│       └── image_generator.rs    #   图片生成
├── static/index.html              # 中国风前端界面
├── data/characters.json           # 汉字知识库（编译期嵌入）
├── Cargo.toml
└── README.md
```

**编译验证（无需 API Key）：**

```bash
cargo build --release
```

看到 `Finished release profile [optimized]` 即表示编译成功。

---

## 3. 配置 API 密钥

在项目根目录创建 `.env` 文件（参考 `.env.example`）：

### 3.1 最简配置（单一服务商）

```bash
# .env
OPENAI_API_KEY=sk-你的密钥
```

就这一行，三个端点（视觉/文本/图片）全部走 OpenAI 官方。

### 3.2 使用第三方中转（如 API2D）

```bash
# .env
OPENAI_API_KEY=sk-你的中转密钥
API_BASE_URL=https://oa.api2d.net/v1
```

### 3.3 各端点独立配置（高级）

```bash
# .env

# ===== 共享默认 =====
OPENAI_API_KEY=sk-共享密钥
API_BASE_URL=https://api.openai.com/v1

# ===== 视觉端点（GPT-4o）=====
VISION_MODEL=gpt-4o
# VISION_API_KEY=sk-视觉专用密钥      # 可选：覆盖共享密钥
# VISION_API_BASE_URL=https://...      # 可选：覆盖共享地址

# ===== 文本端点（gpt-4o-mini）=====
TEXT_MODEL=gpt-4o-mini
# TEXT_API_KEY=sk-文本专用密钥
# TEXT_API_BASE_URL=https://...

# ===== 图片端点（DALL-E 3）=====
IMAGE_MODEL=dall-e-3
IMAGE_SIZE=1024x1024
# IMAGE_API_KEY=sk-图片专用密钥
# IMAGE_API_BASE_URL=https://...

# ===== 通用选项 =====
AUTH_STYLE=bearer              # bearer / header / query
IMAGE_RESPONSE_FORMAT=auto     # url / b64_json / auto
```

> **提示**：`API_BASE_URL` 也可简写为 `API_BASE`，`VISION_API_BASE_URL` 也可简写为 `VISION_API_BASE`，以此类推。

---

## 4. 两种运行模式

### 4.1 Web 服务模式（推荐）

```bash
cargo run --release -- serve          # 默认 3000 端口
cargo run --release -- serve 8080     # 指定端口
```

启动后终端显示：

```
字相 Web 服务启动于 http://127.0.0.1:3000
```

**浏览器打开** `http://127.0.0.1:3000` 即可使用。

> 如果未配置 API Key，服务器仍会启动并托管前端页面，但点击「开始创作」时会提示需要配置密钥。

### 4.2 CLI 命令行模式

```bash
# 默认画风（水墨）
cargo run --release -- 李白 https://example.com/photo.jpg

# 指定画风
cargo run --release -- 李白 https://example.com/photo.jpg 工笔白描
```

| 参数 | 说明 | 必填 |
|------|------|------|
| 姓名 | 中文姓名，如「李白」 | 是 |
| 照片URL | 图片 URL 或 base64 编码 | 是 |
| 画风 | 见[画风说明](#6-画风说明) | 否（默认水墨写意） |

CLI 模式运行后，终端会打印：
1. 生成的完整 Prompt
2. 生成图片的 URL（或本地文件路径）

---

## 5. 使用 Web 界面

### 操作流程

```
打开网页 → 上传照片 → 输入姓名 → 选画风 → 点「开始创作」→ 等待结果
```

**详细步骤：**

1. **上传照片**
   - 点击上传区，选择人物头像照片
   - 或直接拖拽图片到虚线框区域
   - 支持格式：JPG / PNG / WebP
   - 上传后显示缩略图预览

2. **输入姓名**
   - 在输入框中输入中文姓名（如「李白」）
   - 系统会分析姓名中每个汉字的笔画和部首

3. **选择画风**
   - 点击 6 种风格卡片之一
   - 选中项会有高亮边框
   - 默认选中「水墨」

4. **点击「开始创作」**
   - 按钮变为加载状态（旋转动画）
   - 下方显示四步进度：
     - ① 面部特征分析
     - ② 汉字笔画解析
     - ③ 构图规划
     - ④ 图像生成

5. **查看结果**
   - 最终肖像图（圆角带阴影）
   - 三项评分卡片：
     - **面部特征匹配**：面部分析完整度
     - **姓名元素融合**：汉字笔画与部首视觉元素融合度
     - **综合评分**：加权总分
   - 点击「查看生成构思」展开完整 Prompt 文本

### 离线演示模式

直接用浏览器打开 `static/index.html`（即 `file://` 协议访问）：
- 页面正常显示
- 可以上传照片、选风格
- 点击创作后会提示连接失败（因为没有后端服务）
- **适合预览界面设计，不适合实际生成**

---

## 6. 画风说明

| 画风 | 特点 | 适合场景 |
|------|------|---------|
| 水墨 | 墨色浓淡，留白意境，笔触写意 | 传统文人气质 |
| 工笔 | 线条精细，色彩层染，细节入微 | 正式肖像 |
| 油画 | 厚重笔触，光影丰富，色彩饱满 | 西式古典 |
| 赛博朋克 | 霓虹光效，科技质感，未来感 | 个性潮流 |
| 浮世绘 | 平面构图，波浪云纹，日式美学 | 和风艺术 |
| 印象派 | 光影碎笔，色彩朦胧，捕捉瞬间 | 柔和梦幻 |

---

## 7. 多 API 配置详解

「字相」Agent 的三个核心任务可分别使用不同的 API 服务商：

| 任务 | 默认模型 | 可用服务商 |
|------|---------|-----------|
| 视觉分析 | GPT-4o | OpenAI / API2D / OpenRouter / 米醋 等 |
| 文本推理 | gpt-4o-mini | OpenAI / API2D / OpenRouter / 米醋 等 |
| 图片生成 | DALL-E 3 | OpenAI / API2D / 米醋 等兼容接口 |

### 配置优先级

```
分端点变量（VISION_* / TEXT_* / IMAGE_*）
       ↓ 回退
共享变量（OPENAI_API_KEY / API_BASE_URL / ...）
       ↓ 回退
OpenAI 官方默认值
```

### 认证方式

| AUTH_STYLE | 行为 | 适用场景 |
|------------|------|---------|
| `bearer` | `Authorization: Bearer <key>` | OpenAI / API2D（默认） |
| `header` | 自定义头直接放 key | 部分中转服务 |
| `query` | URL 参数 `?api_key=<key>` | 特殊 API |

### 图片响应格式

| IMAGE_RESPONSE_FORMAT | 行为 | 适用场景 |
|-----------------------|------|---------|
| `url` | 直接返回图片 URL | OpenAI 官方 |
| `b64_json` | 返回 base64，自动保存为本地 PNG | 米醋等中转 |
| `auto` | 优先 url，回退 b64_json（默认） | 通用 |

### OpenRouter 示例

```bash
OPENAI_API_KEY=sk-or-你的OpenRouter密钥
API_BASE_URL=https://openrouter.ai/api/v1
EXTRA_HEADERS=HTTP-Referer:https://example.com;X-Title:字相Agent
```

---

## 8. 常见问题

### Q: 编译失败怎么办？

```bash
# 更新 Rust 工具链
rustup update

# 清理后重新编译
cargo clean
cargo build --release
```

### Q: 提示「未配置 API Key」？

检查 `.env` 文件：
- 是否在项目根目录（与 `Cargo.toml` 同级）
- `OPENAI_API_KEY` 是否拼写正确
- 密钥是否以 `sk-` 开头且不为空

### Q: 图片生成很慢？

- DALL-E 3 生成通常需要 10-30 秒
- 如果使用中转服务，延迟可能更高
- 生成过程中请勿关闭页面

### Q: 返回 base64 图片看不到？

后端会自动将 base64 图片转为 data URL 返回给前端。如果仍看不到：
- 检查 `IMAGE_RESPONSE_FORMAT` 设置
- 尝试设为 `auto` 或 `url`

### Q: 如何修改端口？

```bash
cargo run --release -- serve 8888    # 使用 8888 端口
```

### Q: 生成的图片保存在哪里？

- 如果服务商返回 URL：直接是可访问的图片链接
- 如果服务商返回 base64：自动保存为 `字相_时间戳.png` 到工作目录

### Q: 支持哪些图片输入格式？

- HTTP/HTTPS URL：直接使用
- data URL（`data:image/...`）：直接使用
- 裸 base64 字符串：自动补全 data URL 前缀

---

## 快速对照表

| 操作 | 命令 |
|------|------|
| 编译 | `cargo build --release` |
| 启动 Web 服务 | `cargo run --release -- serve` |
| 指定端口 | `cargo run --release -- serve 8080` |
| CLI 生成 | `cargo run --release -- 姓名 照片URL 画风` |

---

> **「字有相，人有相。字相 Agent，让名字和面孔在艺术中相遇。」**
